apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: aegisllm-network-delay
spec:
  action: delay
  mode: one
  selector:
    namespaces: [aegisllm]
  delay:
    latency: '200ms'
    correlation: '50'
    jitter: '50ms'
