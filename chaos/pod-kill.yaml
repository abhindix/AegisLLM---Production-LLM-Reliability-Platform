apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: aegisllm-pod-kill
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces: [aegisllm]
    labelSelectors:
      app: aegisllm
  scheduler:
    cron: '@every 5m'
