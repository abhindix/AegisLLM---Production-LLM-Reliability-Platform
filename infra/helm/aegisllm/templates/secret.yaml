apiVersion: v1
kind: Secret
metadata:
  name: {{ include "aegisllm.fullname" . }}-secrets
type: Opaque
stringData:
  DATABASE_URL: "{{ .Values.secrets.databaseUrl }}"
  AEGIS_API_KEY: "{{ .Values.secrets.apiKey }}"
