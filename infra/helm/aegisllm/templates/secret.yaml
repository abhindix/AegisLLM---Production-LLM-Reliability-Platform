apiVersion: v1
kind: Secret
metadata:
  name: {{ include "aegisllm.fullname" . }}-secrets
type: Opaque
stringData:
  DATABASE_URL: "{{ required "set secrets.databaseUrl" .Values.secrets.databaseUrl }}"
  AEGIS_API_KEY: "{{ required "set secrets.apiKey" .Values.secrets.apiKey }}"
